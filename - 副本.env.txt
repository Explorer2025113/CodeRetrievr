# ==================== 应用基础配置 ====================
APP_NAME=CodeRetrievr
APP_VERSION=1.0.0
DEBUG=True
ENVIRONMENT=development
# ==================== API配置 ====================
API_HOST=0.0.0.0
API_PORT=8000
API_PREFIX=/api/v1
# CORS配置（允许的前端地址，用逗号分隔）
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ==================== 矢量数据库配置 ====================
# Milvus (本地部署)
MILVUS_HOST=localhost
MILVUS_PORT=19530
MILVUS_COLLECTION_NAME=code_snippets
MILVUS_DIMENSION=768

# Pinecone (云端部署) - 二选一，如果使用Pinecone则配置此项
# PINECONE_API_KEY=your_pinecone_api_key_here
# PINECONE_ENVIRONMENT=us-west1-gcp
# PINECONE_INDEX_NAME=code-retrievr

# ==================== 知识图谱配置 ====================
# Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_actual_password_123
# 注意：密码需与 docker-compose.yml 中的 NEO4J_AUTH 配置一致

# ==================== 大模型API配置（DeepSeek推荐） ====================
# 使用DeepSeek（推荐，国内可用，价格优惠）
LLM_PROVIDER=deepseek
LLM_API_KEY=sk-212fc414d08f4b1696bd0a5cef5fafa3
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
LLM_MAX_TOKENS=1000

# ==================== 代码嵌入模型配置 ====================
# 使用本地模型（CodeBERT）
EMBEDDING_MODEL=microsoft/codebert-base
EMBEDDING_DEVICE=cuda
# 如果有GPU，可以设置为 cuda
# ==================== GitHub API配置 ====================
# 用于代码采集，必需
GITHUB_TOKEN=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
GITHUB_RATE_LIMIT=5000
# 获取方式：
# 1. 访问 https://github.com/settings/tokens
# 2. 点击 "Generate new token (classic)"
# 3. 设置权限：public_repo, read:org
# 4. 复制生成的token
# ==================== 数据存储配置 ====================
DATA_DIR=./data
CODE_SNIPPETS_DIR=./data/code_snippets
METADATA_DIR=./data/metadata
# ==================== 缓存配置（可选） ====================
# Redis (可选，用于缓存API响应)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_PASSWORD=

# ==================== 日志配置 ====================
LOG_LEVEL=INFO
# 可选值: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE=./logs/app.log

# ==================== 安全配置 ====================
# JWT密钥（用于用户认证，生产环境必须修改）
SECRET_KEY=your-secret-key-change-in-production-use-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ==================== 部署模式 ====================
# local: 本地部署，使用Milvus
# cloud: 云端部署，使用Pinecone
DEPLOYMENT_MODE=local

# ==================== 示例配置说明 ====================
# 
# 必需配置项：
# 1. LLM_API_KEY - DeepSeek或OpenAI的API密钥（必需）
# 2. GITHUB_TOKEN - GitHub Personal Access Token（必需）
# 3. NEO4J_PASSWORD - Neo4j数据库密码（必需）
#
# 快速开始：
# 1. 复制此文件: cp env.example .env
# 2. 编辑 .env 文件，填入上述三个必需配置
# 3. 运行环境检查: python scripts/check_environment.py
#

